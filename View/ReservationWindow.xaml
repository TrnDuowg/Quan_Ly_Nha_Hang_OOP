<Window x:Class="QuanLyNhaHang.View.ReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QuanLyNhaHang.ViewModel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Đặt Bàn" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        Name="resWindow">

    <Window.DataContext>
        <vm:ReservationViewModel/>
    </Window.DataContext>

    <materialDesign:Card UniformCornerRadius="15" Margin="10" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tiêu đề màu Cam -->
            <Grid Grid.Row="0" Background="#FF9800">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="CalendarClock" Width="30" Height="30" Foreground="White" Margin="0,0,10,0"/>
                    <TextBlock Text="ĐẶT BÀN TRƯỚC" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Form nhập liệu -->
            <StackPanel Grid.Row="1" Margin="30,20">
                <TextBox materialDesign:HintAssist.Hint="Tên khách hàng" Margin="0,10"
                         Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16"/>

                <TextBox materialDesign:HintAssist.Hint="Số điện thoại" Margin="0,10"
                         Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16"/>

                <Grid Margin="0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <DatePicker Grid.Column="0" materialDesign:HintAssist.Hint="Ngày đến" 
                                SelectedDate="{Binding BookingDate}" Margin="0,0,10,0"/>
                    
                    <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Giờ (HH:mm)" 
                             Text="{Binding BookingTime}" Margin="10,0,0,0"/>
                </Grid>

                <TextBox materialDesign:HintAssist.Hint="Số lượng khách" Margin="0,10"
                         Text="{Binding GuestCount}" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16"/>
            </StackPanel>

            <!-- Nút bấm -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                <Button Content="Hủy bỏ" Width="100" Margin="10"
                        Command="{Binding CancelCommand}" 
                        CommandParameter="{Binding ElementName=resWindow}"
                        Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#FF9800" Foreground="#FF9800"/>
                
                <Button Content="XÁC NHẬN" Width="120" Margin="10"
                        Command="{Binding ConfirmCommand}" 
                        CommandParameter="{Binding ElementName=resWindow}"
                        Style="{StaticResource MaterialDesignRaisedButton}" Background="#FF9800" BorderBrush="#FF9800"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>