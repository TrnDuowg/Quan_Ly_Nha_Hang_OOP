<Window x:Class="QuanLyNhaHang.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QuanLyNhaHang.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        
        mc:Ignorable="d"
        Title="Đăng Nhập" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Name="loginWindow">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    
    <!-- Đã xóa Elevation="4" -->
    <materialDesign:Card UniformCornerRadius="15" Background="White" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="ĐĂNG NHẬP" FontSize="30" FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                       HorizontalAlignment="Center" Margin="0,30,0,20"/>

            <StackPanel Grid.Row="1" Margin="40,10">
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Tên đăng nhập"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         FontSize="16"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="40,10">
                <PasswordBox x:Name="FloatingPasswordBox" 
                             materialDesign:HintAssist.Hint="Mật khẩu"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             FontSize="16">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PasswordChanged">
                            <i:InvokeCommandAction Command="{Binding PasswordChangedCommand}" 
                                                   CommandParameter="{Binding ElementName=FloatingPasswordBox}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </PasswordBox>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,30">
                <Button Content="Đăng nhập" Width="120" Height="40" Margin="10"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=loginWindow}"/>

                <Button Content="Thoát" Width="100" Height="40" Margin="10"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding CloseCommand}"
                        CommandParameter="{Binding ElementName=loginWindow}"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>